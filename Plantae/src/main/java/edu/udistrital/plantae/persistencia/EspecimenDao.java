package edu.udistrital.plantae.persistencia;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;
import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;
import edu.udistrital.plantae.logicadominio.datosespecimen.*;
import edu.udistrital.plantae.logicadominio.recoleccion.ColectorPrincipal;
import edu.udistrital.plantae.logicadominio.recoleccion.Viaje;
import edu.udistrital.plantae.logicadominio.ubicacion.Localidad;

import java.util.ArrayList;
import java.util.Date;
import java.util.List;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table ESPECIMEN.
*/
public class EspecimenDao extends AbstractDao<Especimen, Long> {

    public static final String TABLENAME = "ESPECIMEN";

    /**
     * Properties of entity Especimen.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property NumeroDeColeccion = new Property(1, String.class, "numeroDeColeccion", false, "NUMERO_DE_COLECCION");
        public final static Property Abundancia = new Property(2, String.class, "abundancia", false, "ABUNDANCIA");
        public final static Property DescripcionEspecimen = new Property(3, String.class, "descripcionEspecimen", false, "DESCRIPCION_ESPECIMEN");
        public final static Property AlturaDeLaPlanta = new Property(4, Long.class, "alturaDeLaPlanta", false, "ALTURA_DE_LA_PLANTA");
        public final static Property Dap = new Property(5, Long.class, "dap", false, "DAP");
        public final static Property FechaInicial = new Property(6, java.util.Date.class, "fechaInicial", false, "FECHA_INICIAL");
        public final static Property FechaFinal = new Property(7, java.util.Date.class, "fechaFinal", false, "FECHA_FINAL");
        public final static Property MetodoColeccion = new Property(8, String.class, "metodoColeccion", false, "METODO_COLECCION");
        public final static Property EstacionDelAño = new Property(9, String.class, "estacionDelAño", false, "ESTACION_DEL_AÑO");
        public final static Property HabitoID = new Property(10, Long.class, "habitoID", false, "HABITO_ID");
        public final static Property HabitatID = new Property(11, Long.class, "habitatID", false, "HABITAT_ID");
        public final static Property FenologiaID = new Property(12, Long.class, "fenologiaID", false, "FENOLOGIA_ID");
        public final static Property LocalidadID = new Property(13, Long.class, "localidadID", false, "LOCALIDAD_ID");
        public final static Property ViajeID = new Property(14, long.class, "viajeID", false, "VIAJE_ID");
        public final static Property ColectorPrincipalID = new Property(15, long.class, "colectorPrincipalID", false, "COLECTOR_PRINCIPAL_ID");
        public final static Property RaizID = new Property(16, Long.class, "raizID", false, "RAIZ_ID");
        public final static Property TalloID = new Property(17, Long.class, "talloID", false, "TALLO_ID");
        public final static Property InflorescenciaID = new Property(18, Long.class, "inflorescenciaID", false, "INFLORESCENCIA_ID");
        public final static Property FrutoID = new Property(19, Long.class, "frutoID", false, "FRUTO_ID");
        public final static Property FlorID = new Property(20, Long.class, "florID", false, "FLOR_ID");
        public final static Property HojaID = new Property(21, Long.class, "hojaID", false, "HOJA_ID");
        public final static Property Tipo = new Property(22, String.class, "tipo", false, "TIPO");
    };

    private DaoSession daoSession;

    private Query<Especimen> colectorPrincipal_EspecimenesQuery;
    private Query<Especimen> viaje_EspecimenesQuery;

    public EspecimenDao(DaoConfig config) {
        super(config);
    }
    
    public EspecimenDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'ESPECIMEN' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'NUMERO_DE_COLECCION' TEXT," + // 1: numeroDeColeccion
                "'ABUNDANCIA' TEXT," + // 2: abundancia
                "'DESCRIPCION_ESPECIMEN' TEXT," + // 3: descripcionEspecimen
                "'ALTURA_DE_LA_PLANTA' INTEGER," + // 4: alturaDeLaPlanta
                "'DAP' INTEGER," + // 5: dap
                "'FECHA_INICIAL' INTEGER," + // 6: fechaInicial
                "'FECHA_FINAL' INTEGER," + // 7: fechaFinal
                "'METODO_COLECCION' TEXT," + // 8: metodoColeccion
                "'ESTACION_DEL_AÑO' TEXT," + // 9: estacionDelAño
                "'HABITO_ID' INTEGER," + // 10: habitoID
                "'HABITAT_ID' INTEGER," + // 11: habitatID
                "'FENOLOGIA_ID' INTEGER," + // 12: fenologiaID
                "'LOCALIDAD_ID' INTEGER," + // 13: localidadID
                "'VIAJE_ID' INTEGER NOT NULL ," + // 14: viajeID
                "'COLECTOR_PRINCIPAL_ID' INTEGER NOT NULL ," + // 15: colectorPrincipalID
                "'RAIZ_ID' INTEGER," + // 16: raizID
                "'TALLO_ID' INTEGER," + // 17: talloID
                "'INFLORESCENCIA_ID' INTEGER," + // 18: inflorescenciaID
                "'FRUTO_ID' INTEGER," + // 19: frutoID
                "'FLOR_ID' INTEGER," + // 20: florID
                "'HOJA_ID' INTEGER," + // 21: hojaID
                "'TIPO' TEXT);"); // 22: tipo
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_NUMERO_DE_COLECCION ON ESPECIMEN" +
                " (NUMERO_DE_COLECCION);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_HABITO_ID ON ESPECIMEN" +
                " (HABITO_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_HABITAT_ID ON ESPECIMEN" +
                " (HABITAT_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_FENOLOGIA_ID ON ESPECIMEN" +
                " (FENOLOGIA_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_LOCALIDAD_ID ON ESPECIMEN" +
                " (LOCALIDAD_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_VIAJE_ID ON ESPECIMEN" +
                " (VIAJE_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_COLECTOR_PRINCIPAL_ID ON ESPECIMEN" +
                " (COLECTOR_PRINCIPAL_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_RAIZ_ID ON ESPECIMEN" +
                " (RAIZ_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_TALLO_ID ON ESPECIMEN" +
                " (TALLO_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_INFLORESCENCIA_ID ON ESPECIMEN" +
                " (INFLORESCENCIA_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_FRUTO_ID ON ESPECIMEN" +
                " (FRUTO_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_FLOR_ID ON ESPECIMEN" +
                " (FLOR_ID);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_ESPECIMEN_HOJA_ID ON ESPECIMEN" +
                " (HOJA_ID);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'ESPECIMEN'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Especimen entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String numeroDeColeccion = entity.getNumeroDeColeccion();
        if (numeroDeColeccion != null) {
            stmt.bindString(2, numeroDeColeccion);
        }
 
        String abundancia = entity.getAbundancia();
        if (abundancia != null) {
            stmt.bindString(3, abundancia);
        }
 
        String descripcionEspecimen = entity.getDescripcionEspecimen();
        if (descripcionEspecimen != null) {
            stmt.bindString(4, descripcionEspecimen);
        }
 
        Long alturaDeLaPlanta = entity.getAlturaDeLaPlanta();
        if (alturaDeLaPlanta != null) {
            stmt.bindLong(5, alturaDeLaPlanta);
        }
 
        Long dap = entity.getDap();
        if (dap != null) {
            stmt.bindLong(6, dap);
        }
 
        Date fechaInicial = entity.getFechaInicial();
        if (fechaInicial != null) {
            stmt.bindLong(7, fechaInicial.getTime());
        }
 
        Date fechaFinal = entity.getFechaFinal();
        if (fechaFinal != null) {
            stmt.bindLong(8, fechaFinal.getTime());
        }

        String metodoColeccion = entity.getMetodoColeccion();
        if (metodoColeccion != null) {
            stmt.bindString(9, metodoColeccion);
        }

        String estacionDelAño = entity.getEstacionDelAño();
        if (estacionDelAño != null) {
            stmt.bindString(10, estacionDelAño);
        }

        Long habitoID = entity.getHabitoID();
        if (habitoID != null) {
            stmt.bindLong(11, habitoID);
        }
 
        Long habitatID = entity.getHabitatID();
        if (habitatID != null) {
            stmt.bindLong(12, habitatID);
        }
 
        Long fenologiaID = entity.getFenologiaID();
        if (fenologiaID != null) {
            stmt.bindLong(13, fenologiaID);
        }

        Long localidadID = entity.getLocalidadID();
        if (localidadID != null) {
            stmt.bindLong(14, localidadID);
        }
        stmt.bindLong(15, entity.getViajeID());
        stmt.bindLong(16, entity.getColectorPrincipalID());

        Long raizID = entity.getRaizID();
        if (raizID != null) {
            stmt.bindLong(17, raizID);
        }
 
        Long talloID = entity.getTalloID();
        if (talloID != null) {
            stmt.bindLong(18, talloID);
        }
 
        Long inflorescenciaID = entity.getInflorescenciaID();
        if (inflorescenciaID != null) {
            stmt.bindLong(19, inflorescenciaID);
        }
 
        Long frutoID = entity.getFrutoID();
        if (frutoID != null) {
            stmt.bindLong(20, frutoID);
        }
 
        Long florID = entity.getFlorID();
        if (florID != null) {
            stmt.bindLong(21, florID);
        }
 
        Long hojaID = entity.getHojaID();
        if (hojaID != null) {
            stmt.bindLong(22, hojaID);
        }

        String tipo = entity.getTipo();
        if (tipo != null) {
            stmt.bindString(23, tipo);
        }
    }

    @Override
    protected void attachEntity(Especimen entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Especimen readEntity(Cursor cursor, int offset) {
        Especimen entity = new Especimen();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Especimen entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNumeroDeColeccion(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAbundancia(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDescripcionEspecimen(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setAlturaDeLaPlanta(cursor.isNull(offset + 4) ? null : cursor.getLong(offset + 4));
        entity.setDap(cursor.isNull(offset + 5) ? null : cursor.getLong(offset + 5));
        entity.setFechaInicial(cursor.isNull(offset + 6) ? null : new java.util.Date(cursor.getLong(offset + 6)));
        entity.setFechaFinal(cursor.isNull(offset + 7) ? null : new java.util.Date(cursor.getLong(offset + 7)));
        entity.setMetodoColeccion(cursor.isNull(offset + 8) ? null : cursor.getString(offset + 8));
        entity.setEstacionDelAño(cursor.isNull(offset + 9) ? null : cursor.getString(offset + 9));
        entity.setHabitoID(cursor.isNull(offset + 10) ? null : cursor.getLong(offset + 10));
        entity.setHabitatID(cursor.isNull(offset + 11) ? null : cursor.getLong(offset + 11));
        entity.setFenologiaID(cursor.isNull(offset + 12) ? null : cursor.getLong(offset + 12));
        entity.setLocalidadID(cursor.isNull(offset + 13) ? null : cursor.getLong(offset + 13));
        entity.setViajeID(cursor.getLong(offset + 14));
        entity.setColectorPrincipalID(cursor.getLong(offset + 15));
        entity.setRaizID(cursor.isNull(offset + 16) ? null : cursor.getLong(offset + 16));
        entity.setTalloID(cursor.isNull(offset + 17) ? null : cursor.getLong(offset + 17));
        entity.setInflorescenciaID(cursor.isNull(offset + 18) ? null : cursor.getLong(offset + 18));
        entity.setFrutoID(cursor.isNull(offset + 19) ? null : cursor.getLong(offset + 19));
        entity.setFlorID(cursor.isNull(offset + 20) ? null : cursor.getLong(offset + 20));
        entity.setHojaID(cursor.isNull(offset + 21) ? null : cursor.getLong(offset + 21));
        entity.setTipo(cursor.isNull(offset + 22) ? null : cursor.getString(offset + 22));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Especimen entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Especimen entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "especimenes" to-many relationship of ColectorPrincipal. */
    public List<Especimen> _queryColectorPrincipal_Especimenes(long colectorPrincipalID) {
        synchronized (this) {
            if (colectorPrincipal_EspecimenesQuery == null) {
                QueryBuilder<Especimen> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ColectorPrincipalID.eq(null));
                colectorPrincipal_EspecimenesQuery = queryBuilder.build();
            }
        }
        Query<Especimen> query = colectorPrincipal_EspecimenesQuery.forCurrentThread();
        query.setParameter(0, colectorPrincipalID);
        return query.list();
    }

    /** Internal query to resolve the "especimenes" to-many relationship of Viaje. */
    public List<Especimen> _queryViaje_Especimenes(long viajeID) {
        synchronized (this) {
            if (viaje_EspecimenesQuery == null) {
                QueryBuilder<Especimen> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.ViajeID.eq(null));
                viaje_EspecimenesQuery = queryBuilder.build();
            }
        }
        Query<Especimen> query = viaje_EspecimenesQuery.forCurrentThread();
        query.setParameter(0, viajeID);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getHabitoDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getHabitatDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getFenologiaDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T3", daoSession.getLocalidadDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T4", daoSession.getInflorescenciaDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T5", daoSession.getColectorPrincipalDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T6", daoSession.getViajeDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T7", daoSession.getHojaDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T8", daoSession.getFrutoDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T9", daoSession.getTalloDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T10", daoSession.getRaizDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T11", daoSession.getFlorDao().getAllColumns());
            builder.append(" FROM ESPECIMEN T");
            builder.append(" LEFT JOIN HABITO T0 ON T.'HABITO_ID'=T0.'_id'");
            builder.append(" LEFT JOIN HABITAT T1 ON T.'HABITAT_ID'=T1.'_id'");
            builder.append(" LEFT JOIN FENOLOGIA T2 ON T.'FENOLOGIA_ID'=T2.'_id'");
            builder.append(" LEFT JOIN LOCALIDAD T3 ON T.'LOCALIDAD_ID'=T3.'_id'");
            builder.append(" LEFT JOIN INFLORESCENCIA T4 ON T.'INFLORESCENCIA_ID'=T4.'_id'");
            builder.append(" LEFT JOIN COLECTOR_PRINCIPAL T5 ON T.'COLECTOR_PRINCIPAL_ID'=T5.'_id'");
            builder.append(" LEFT JOIN VIAJE T6 ON T.'VIAJE_ID'=T6.'_id'");
            builder.append(" LEFT JOIN HOJA T7 ON T.'HOJA_ID'=T7.'_id'");
            builder.append(" LEFT JOIN FRUTO T8 ON T.'FRUTO_ID'=T8.'_id'");
            builder.append(" LEFT JOIN TALLO T9 ON T.'TALLO_ID'=T9.'_id'");
            builder.append(" LEFT JOIN RAIZ T10 ON T.'RAIZ_ID'=T10.'_id'");
            builder.append(" LEFT JOIN FLOR T11 ON T.'FLOR_ID'=T11.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Especimen loadCurrentDeep(Cursor cursor, boolean lock) {
        Especimen entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Habito habito = loadCurrentOther(daoSession.getHabitoDao(), cursor, offset);
        entity.setHabito(habito);
        offset += daoSession.getHabitoDao().getAllColumns().length;

        Habitat habitat = loadCurrentOther(daoSession.getHabitatDao(), cursor, offset);
        entity.setHabitat(habitat);
        offset += daoSession.getHabitatDao().getAllColumns().length;

        Fenologia fenologia = loadCurrentOther(daoSession.getFenologiaDao(), cursor, offset);
        entity.setFenologia(fenologia);
        offset += daoSession.getFenologiaDao().getAllColumns().length;

        Localidad localidad = loadCurrentOther(daoSession.getLocalidadDao(), cursor, offset);
        entity.setLocalidad(localidad);
        offset += daoSession.getLocalidadDao().getAllColumns().length;

        Inflorescencia inflorescencia = loadCurrentOther(daoSession.getInflorescenciaDao(), cursor, offset);
        entity.setInflorescencia(inflorescencia);
        offset += daoSession.getInflorescenciaDao().getAllColumns().length;

        ColectorPrincipal colectorPrincipal = loadCurrentOther(daoSession.getColectorPrincipalDao(), cursor, offset);
         if(colectorPrincipal != null) {
            entity.setColectorPrincipal(colectorPrincipal);
        }
        offset += daoSession.getColectorPrincipalDao().getAllColumns().length;

        Viaje viaje = loadCurrentOther(daoSession.getViajeDao(), cursor, offset);
         if(viaje != null) {
            entity.setViaje(viaje);
        }
        offset += daoSession.getViajeDao().getAllColumns().length;

        Hoja hoja = loadCurrentOther(daoSession.getHojaDao(), cursor, offset);
        entity.setHoja(hoja);
        offset += daoSession.getHojaDao().getAllColumns().length;

        Fruto fruto = loadCurrentOther(daoSession.getFrutoDao(), cursor, offset);
        entity.setFruto(fruto);
        offset += daoSession.getFrutoDao().getAllColumns().length;

        Tallo tallo = loadCurrentOther(daoSession.getTalloDao(), cursor, offset);
        entity.setTallo(tallo);
        offset += daoSession.getTalloDao().getAllColumns().length;

        Raiz raiz = loadCurrentOther(daoSession.getRaizDao(), cursor, offset);
        entity.setRaiz(raiz);
        offset += daoSession.getRaizDao().getAllColumns().length;

        Flor flor = loadCurrentOther(daoSession.getFlorDao(), cursor, offset);
        entity.setFlor(flor);

        return entity;    
    }

    public Especimen loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Especimen> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Especimen> list = new ArrayList<Especimen>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Especimen> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Especimen> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
